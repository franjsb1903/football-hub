import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ['media.api-sports.io'], // Agrega los dominios de tus imágenes externas aquí
	},
}

export default nextConfig
