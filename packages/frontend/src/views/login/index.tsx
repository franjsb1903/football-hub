import styles from './styles.module.css'

export default function Login() {
	return (
		<section>
			<h1 className={styles.some}>Login</h1>
			<p>Login page</p>
			<button>Login</button>
		</section>
	)
}
